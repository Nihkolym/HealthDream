const path = require("path");

module.exports = {
  "config": path.resolve("./src/lib/db/config", "config.json"),
  "models-path": path.resolve("build", "src", "lib", "db", "models"),
  "seeders-path": path.resolve("build", "src", "lib", "db", "seeders")
};
